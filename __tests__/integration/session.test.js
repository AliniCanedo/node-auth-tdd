// First parameter test name
// Second parameter function which is the test itself
describe('Authentication', () => {
  it('should receive JWT token when authenticated with valid credentials', () => {});
  it('should sum two number', () => {
    const x = 2;
    const y = 4;

    const sum = x + y;
    expect(sum).toBe(6);
  });
});
